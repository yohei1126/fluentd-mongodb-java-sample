apply plugin: 'java'
apply plugin: 'application'

repositories {
    jcenter()
}

dependencies {
    compile 'org.fluentd:fluent-logger:0.3.1'
    testCompile 'junit:junit:4.12'
}

mainClassName = 'FluentdLogger'

startScripts {
    applicationName = 'fluentd-logger'
}
